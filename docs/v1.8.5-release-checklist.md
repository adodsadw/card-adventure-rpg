# v1.8.5 發佈檢查

1. `worker/wrangler.toml` 的 `main` 為 `src/app.js`。
2. `/api/health` 回傳 `1.8.5`。
3. 管理後台標題與登入畫面顯示 `v1.8.5`。
4. `frontend/sw.js` 快取名稱為 `starrealm-v1.8.5`。
5. GM 總覽可讀取玩家、資源與管理紀錄。
6. 本版不需要執行 D1 Migration。
